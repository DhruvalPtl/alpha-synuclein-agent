# Workspace Rules

- **Python Virtual Environment**: Always use the virtual environment located at `.venv` in the project folder. For command executions on Windows, reference the python/pip executables explicitly at `.venv\Scripts\python.exe` and `.venv\Scripts\pip.exe`.
